#echo ".bash_login"
source ~/.aliases
source ~/.swear

source ~/.git-completion.sh
source ~/.git-prompt.sh
export PS0='\h $(__git_ps1 "{%s}") - '

[[ -r ~/venv/bin/activate ]] && source ~/venv/bin/activate
